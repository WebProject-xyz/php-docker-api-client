on:
  push:
    branches:
      - "main"
  pull_request:

name: "CI"

jobs:
  ci:
    uses: "WebProject-xyz/.github/.github/workflows/ci.yml@main"
    secrets: inherit
    with:
      php-cs-fixer-ignore-env: true
