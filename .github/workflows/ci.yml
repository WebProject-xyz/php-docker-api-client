on:
  push:
    branches:
      - "main"
  pull_request:

name: "CI"

permissions:
  contents: read
  actions: read
  security-events: write

jobs:
  ci:
    uses: "WebProject-xyz/.github/.github/workflows/ci.yml@065145ee935f212f66adfeaecb73ed5ad365877e" # main
    secrets: inherit
    with:
      php-cs-fixer-ignore-env: true
      test-matrix: |
        [
          {"php-version": "8.3", "dependencies": "locked",  "with_coverage": true},
          {"php-version": "8.4", "dependencies": "highest", "with_coverage": false},
          {"php-version": "8.5", "dependencies": "highest", "with_coverage": false}
        ]
